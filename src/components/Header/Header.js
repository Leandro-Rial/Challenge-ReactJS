import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => {
    return (
        <div>
            <nav>
                <Link to="/">Logo
                </Link>
            </nav>
        </div>
    )
}

export default Header
